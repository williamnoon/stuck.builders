# STUCK TO SHIPPED — MASTER PRD
*Source of truth. Everything — landing pages, forms, ads, delivery — is built from this.*

---

## 1. THE ONE-LINE PRODUCT

Stuck.builders is one job: **builders getting unstuck and shipping the next version.** Two products, both Builds. **Greenfield Build** ($199) when there's no code yet — the imaginative solution + PRD + AI-native demo + operating environment. **Brownfield Build** ($399) when there's an existing project — unstuck + deployed live next version. **Skool ($69/mo or $500/yr, 7-day free trial)** is the walking-pace continuity layer, not a third product.

*Greenfield = no code yet. Brownfield = existing project. Users may not know the engineering terms; every page defines them visually + textually.*

---

## 2. THE LADDER (the whole business, one view)

```
$199 GREENFIELD BUILD  →  $399 BROWNFIELD BUILD (core)  →  $69/mo or $500/yr SKOOL
                                                            (7-day free trial)
      │                          │                                │
   imaginative                 unstuck + deployed              weekly Labs,
   solution + PRD +            live next version               searchable library,
   AI-native demo +            (one major feature               community feed
   operating env               or major fix)
      │                          │                                │
      └──────────────────────────┴────────────────────────────────┘
                                 ↓
                       THE FORK (after any sprint)
                       ┌──────────────┴──────────────┐
                 KEEP SPRINTING              DROP TO COMMUNITY
                 ($399, next wall)           ($69/mo · $500/yr, walking pace)
```

**One job. Two intensities.** 1-on-1 sprint = you *and* Will on your project, dedicated 7-day engine. Skool = same work in live group Labs you can watch, hot-seat right when picked.

**"Launch Sprint" is retired.** Traction is just a *next version* — a Brownfield Build whose Day-7 target = "first 10 users reached" is still a Brownfield Build. The `/brownfield#traction` hero is a Brownfield variant angled at "shipped, no users" (parallel to how `/brownfield#ceiling` is a Brownfield variant angled at Lovable/Bolt ceiling-hitters).

**Customer lifetime value (full chain):** $199 + $399 + $69/mo recurring (or $500/yr) = **$598 upfront + $69/mo (or $500/yr) indefinitely**.

---

## 3. THE TWO SPRINT MODES

**Same 7-day engine, same runbook, same calendar. Only the work inside the containers changes. Every sprint runs on the same productized setup-to-win operating environment — prompts, docs, tools, skills we build together and the customer keeps using after.**

### 3A. GREENFIELD BUILD — $199
- **Who:** "I have an idea to build something of value" / "I have an idea but I'm stuck on how to build it" / "I just learned how to use AI/Bolt but I don't know what to build" / **"I have AI. I still don't know what to build."**
- **Adversity:** They aren't a builder yet. They need to become one. AI democratized building. Almost nobody can conceive an idea worth building.
- **Core deliverable — THE IMAGINATIVE SOLUTION.** The imaginative solution is the shape of an idea worth building, developed with Will's taste + judgment across Day 1 background-mining and Days 2–6 of pressure-testing against real people. The PRD, the demo, and the operating environment are **artifacts** of that solution — the delivery vehicle, not the outcome.
- **Day 1 (~30 min call):** Background-mine — Will interviews *you* on your background, interests, and problems close to you. Pressure-test the idea against the four filters (§6). Begin shaping the imaginative solution.
- **Days 2–6 (30–60 min daily):** Set up the operating environment together (prompts, docs, tools, skills) and build the AI-native demo of the solution. Hands on the customer's keyboard — Will guides and demonstrates AI-native moves, but the customer does the work.
- **Day 7 (45 min):** Deliver the **imaginative solution** as: the **PRD** (1–2 pages, spec to build from) + the **built AI-native demo** (link to a running MVP of the solution) + the operating environment they keep + next-3-steps.
- **Delivery style:** They code, Will copilots. Never done-for-you.
- **Guarantee stamp:** `SOLUTION + DEMO / OR REFUND`. If you don't walk away with an imaginative solution you believe is worth building, a working AI-native demo of it, and the operating environment we built together by the Day 7 delivery call — full refund. An honest "don't build this, here's what to build instead" (with the demo as a pivot artifact) still counts as delivery.
- **Feeds:** the $399 Brownfield Build (they now have something built = qualified) or Skool.

### 3B. BROWNFIELD BUILD — $399  ← CORE PRODUCT
- **Who:** "I started building. It's stuck somewhere." Includes Lovable/Bolt/v0/Replit users hitting the ceiling (`/brownfield#ceiling`) and shipped-no-users builders needing traction (`/brownfield#traction`).
- **Adversity:** Unblock + ship the next version.
- **Day 1 scoping (~45 min call):** Lock two things: (a) where they're stuck **in their own words** (their language, not our re-framing), (b) what we're building towards. Scope target = **one major feature or one major fix.** No mid-sprint renegotiation.
- **Days 2–6:** Adaptive daily cadence (rhythm shaped by what the scoped feature/fix needs — SWE judgment on the day-by-day, not a fixed template). Hands on their keyboard, Will copilots. Momentum never stops.
- **Unstick calls:** up to 5/week, up to 10 min each — when they hit a wall between sessions.
- **Day 7 (30 min):** Ship review. Demo the deployed app + real URL. Recordings archived + next-version roadmap.
- **Deliverable:** **Unstuck + deployed live next version at a real URL.** Non-negotiable — every Brownfield Build ships deployed.
- **Delivery style:** They code, Will copilots. Never done-for-you.
- **Feeds:** another Brownfield Build (next version) or Skool.

---

## 4. SKOOL COMMUNITY — $69/mo or $500/yr (7-day free trial)

- **What:** Same operating engine at group pace. Weekly **Labs** (live group work sessions, ~5/week) where everyone can *watch* the live and 5 hot seats per Lab get *worked on*. Members submit their project inside the community ahead of a scheduled Lab; Will picks the 5 by Sunday for the following week. Searchable library organized by the four-stage problem taxonomy (Idea → Build → Deploy → Market). Async community feed between Labs.
- **Entry (three states, one door):** "You have an idea you've never started. Or an app that's half-built and fighting you. Or something you shipped that nobody uses. Stuck to Shipped gets you unstuck — and you feel it the same week." (Verbatim from the Skool about page. Do not paraphrase in ads or pages without checking against this.)
- **Position:** The "walking pace" alternative to sprinting. Not sold cold as the front door — it's the back-end that retains sprint graduates. Positioning line: *"Nobody leaves where they came in."*
- **Pricing (live on Skool platform):** $69/mo or $500/yr (~$42/mo effective, saves $328/yr). **7-day free trial.** No founding tier — the founding-20 model was superseded 2026-07-09.
- **Sprint upsell inside Skool copy:** *"Need a breakthrough THIS week? Book a private Sprint → stuck.builders"*.
- **Members apply for hot seats** — same selection logic as sprints (§5), 5 per Lab. Hot-seat applications close Sunday for the following week's Lab — this SLA is different from sprint-application SLA (§5).
- **Two separate caps, both = 5, do NOT conflate in copy:** (a) 5 hot seats per Skool Lab, (b) 5 concurrent active paid sprints **per SKU** (5 Greenfield + 5 Brownfield, up to 10 total sprints in motion).

---

## 5. THE APPLICATION MODEL (critical — sprints are APPLIED FOR, not just bought)

**You cannot simply buy a slot. Slots are limited (5 concurrent per SKU) and applied for.** Two reasons: genuine capacity limit, and Will works only on projects he believes in and that clear the ethics line (§6).

### The flow:
1. Visitor picks their sprint (routed by page / cross-link)
2. Fills the **Sprint Application Form** (§7)
3. Application reviewed → accepted applicants get the payment link / booking
4. Payment locks the slot; sprint starts Monday

**Approval SLA: ~5 minutes.** Will reads applications live. The promise is: apply now, hear back before you close the tab. Exception: occasionally a few hours if Will is inside a Day-1 session or asleep — but never a full day.

**Why apply-then-pay, not pay-then-intake:** protects Will's time and standards, creates real scarcity (5 slots per SKU, first-accepted-first-locked), and filters out projects that don't clear the ethics line before money changes hands.

*(Skool hot-seat SLA is different — hot seats are picked by Sunday for the following week's Lab. Do not apply the ~5 min sprint SLA to hot seats.)*

*(Note: if apply-first proves too slow for ad conversion, fallback is pay-first with a refund guarantee for rejected projects — but default to apply-first for launch.)*

---

## 6. THE FOUR FILTERS (Will's ideation + acceptance lens)

Every project is pressure-tested against these. The first is a hard line; the rest are coaching judgment:

1. **Not illegal / not harmful / not predatory** — HARD LINE. Projects involving illegal activity or harm are declined, no exceptions. State this on the form.
2. **Solves a real problem** — not a problem invented to justify the idea.
3. **Valuable / beneficial** — deserves to exist, worth someone's attention.
4. **Close to you** — you have background, interest, or lived proximity to the problem.

Filter 1 is a gate (voids acceptance). Filters 2–4 are Will's lens for coaching and for choosing projects he believes in — applied with judgment, not as automatic rejections.

---

## 7. THE FORMS (what gets built)

### Sprint Application Form (both sprints, one form, branches on Q1)
- **Q1 — Where are you?**
  - [ ] **Greenfield — I have/need idea. Nothing built yet. — $199**
  - [ ] **Brownfield — I started building. I'm stuck. — $399**
- **Q2 — Your project:** describe it in a few sentences **OR paste a link** (repo, live URL, Lovable/Bolt project, Figma, doc — anything that shows it)
- **Q3 (GREENFIELD path only) — About you:** your background, your interests, problems close to you that you wish were solved
- **Q4 — Where exactly are you stuck?** (2–3 sentences, in your own words — this becomes the locked scope on Day 1)
- **Q5 — What would "shipped" look like for you by Day 7?**
- **Q6 — Contact:** name, email
- **Referral code (optional):** auto-filled from `ref_code` cookie if present
- **Acknowledgment checkbox:** "I understand projects must be legal and non-harmful, and that slots are limited and applications are reviewed."

Form promise (`.form-hint`): *"Reply in ~5 min — accepted or not. Will reads live."*

*(Note: "shipped, no users" builders enter through the Brownfield path. Their traction milestone is what becomes the Day-1 scoped "one major feature or fix" — e.g., "first 10 users reached, one angle proven live" is a legitimate Brownfield Build scope.)*

### Post-acceptance:
- Accepted → payment link (Stripe, Cash App Pay enabled — with $100 credit applied if `ref_code` present) → booking link for Day 1 → sprint starts Monday.

---

## 8. THE PAGES (what needs building)

| Page | URL | Sells | Deliverable promised | Cross-link |
|---|---|---|---|---|
| ROOT redirect | `/` | — | 302 → `/brownfield` | — |
| BROWNFIELD (core) | `/brownfield` | $399 Brownfield Build | Unstuck + deployed live next version | → "no code yet? Greenfield Build" |
| BROWNFIELD — Ceiling variant | `/brownfield#ceiling` | $399 Brownfield Build (Lovable/Bolt ceiling angle) | Unstuck + deployed live next version, owned code | → same |
| BROWNFIELD — Traction variant | `/brownfield#traction` | $399 Brownfield Build (shipped-no-users angle) | Unstuck + deployed live next version, first users reached | → same |
| GREENFIELD | `/greenfield` | $199 Greenfield Build | Imaginative solution + PRD + AI-native demo + operating env | → "already building? Brownfield Build" |
| Skool community | `/community` (+ Skool URL) | $69/mo or $500/yr, 7-day trial | Weekly Labs + community | offered at Day 7 |

**Retired URLs (301 redirects):** `/idea` → `/greenfield`, `/out` → `/brownfield#ceiling`, `/grow` → `/brownfield#traction`.

**All pages share:** the brand system, the hand-annotated aesthetic, the slot board (5 Greenfield slots + 5 Brownfield slots as two independent counters, current + next week), the application CTA (not raw checkout — applies via form), the price with the four-filter honesty, the Skool-at-Day-7 note, the referral banner (*"Referred by a builder? Add their code at apply. Both of you get $100 off."*).

**Copy principle (hard rule):** every word informs or helps the reader act. No hype. Handwritten notes carry information or mirror the customer's inner voice.

**Marketing honesty (the fix):** each page promises its REAL deliverable. GREENFIELD promises an imaginative solution + PRD + built AI-native demo (MVP-grade), not a production app. BROWNFIELD promises deployed live next version. Skool promises weekly live Labs pace. Never sell "app in 7 days" on the GREENFIELD page, never sell "we build it for you" on any page.

**Operator identity across the site:** *"a builder who ships with AI every day, with taste about what deserves to be built."* AI-native, not consultant. The AI-native half validates capability (Brownfield). The taste half validates judgment (Greenfield).

---

## 9. THE ADS (two tracks, run now)

**Track A — Sprint (front door, cold traffic):**
- **BROWNFIELD** ad → `/brownfield` — "You built the app. Then you hit the wall." → deployed live next version in 7 days
- **BROWNFIELD-CEILING** ad → `/brownfield#ceiling` — "It worked until you added login." (Lovable/Bolt) → deployed live next version, owned code
- **BROWNFIELD-TRACTION** ad → `/brownfield#traction` — "You built it. Nobody's using it." → deployed live next version + first users reached
- **GREENFIELD** ad → `/greenfield` — "AI democratized building. Almost nobody can conceive an idea worth building. In 7 days, ship the imaginative solution + a working demo." → imaginative solution + AI-native MVP
- **Approval SLA angle (all campaigns):** "Apply, hear back before you close the tab."
- **Referral angle (cross-cutting):** "Bring a builder. Both of you get $100 off."
- Budget: $50/day, Sales objective, Lead event (see §10), broad US, age 24–55, 3 creatives, Advantage+ on.

**Track B — Skool (community):**
- "You have an idea you've never started. Or an app that's half-built. Or something you shipped nobody uses. Get unstuck live, weekly. $69/mo or $500/yr — 7-day free trial." → Skool join
- Run lighter; community sells best to warm/sprint grads first.

---

## 10. TECHNICAL NOTES FOR BUILD

- **Apply-first changes the pixel event:** optimize ads for a **Lead** event (application submitted) at first, since Purchase happens after acceptance. Once volume grows, can test Purchase optimization.
- **Stripe Payment Links** with Cash App Pay enabled, collect email, redirect to booking.
- **Forms:** Tally or Typeform (fast) or custom on the Next.js stack. Application form → review queue (even just an email/Airtable to start).
- **Slot board honesty:** track two independent counters (5 Greenfield + 5 Brownfield). Update filled/open state as slots fill; a real sold-out counter becomes ad creative.
- **Domains:** stuck.builders primary (ad landing), stuck2shipped.builders secondary/SEO.
- **Calendar:** Cal.com, event types = Day 1 Scoping (30 min GREENFIELD / 45 min BROWNFIELD), Daily Session (15–30 min, two daily windows), Unstick Call (10 min), Lab (Skool live).
- **Prices in code:** `apps/web/lib/config.ts` → `PRICES` map (`idea: 199, build: 399` — internal keys retained; external labels are "Greenfield Build" and "Brownfield Build"). Skool constants: `SKOOL_PRICE_MONTHLY = 69`, `SKOOL_PRICE_ANNUAL = 500`, `SKOOL_TRIAL_DAYS = 7`. `SKOOL_FOUNDING_CAP` retired.
- **Referral tracking:** `?ref=CODE` URL param → cookie `ref_code` (60 days) → included in application POST payload → included in acceptance email; Will manually generates the discounted Stripe link.
- **UTM tracking:** application form captures `utm_source`, `utm_campaign`, `utm_content` and includes them in the Meta Pixel Lead event + application email.

---

## 11. LAUNCH PRIORITY (concise, ship now)

**Phase 1 — TODAY (get ads running):**
1. BROWNFIELD page live at stuck.builders/brownfield (adjust CTA to "Apply for a slot")
2. Application form live (branches on Q1 — two options only)
3. Stripe link ready for accepted applicants ($199 Greenfield, $399 Brownfield)
4. Pixel + Lead event on form submit
5. BROWNFIELD ad campaign live, $50/day

**Phase 2 — this week:**
6. GREENFIELD page (`/greenfield`) + BROWNFIELD-CEILING (`/brownfield#ceiling`) + BROWNFIELD-TRACTION (`/brownfield#traction`) all live
7. Skool community stood up + $69/mo · $500/yr · 7-day trial live
8. Runbook split into two path workflows (Greenfield + Brownfield)

**Phase 3 — after first sprint customers:**
9. Testimonials → new ad creative
10. CAPI server-side, live slot board with two counters
11. Referral tracking dashboard

---

## 12. REFERRAL PROGRAM

**Both parties get $100 credit.** Open to anyone — Ship Club member, sprint grad, first-time visitor. Never expires. Applies once per referred person.

**Mechanics:**
- Any visitor can generate a personal referral link from `/community` or the Skool space.
- Format: `stuck.builders/?ref=CODE` (redirects to `/brownfield` by default; carries the code as a cookie for 60 days).
- **Referred party gets $100 credit** applied to their first Greenfield ($99) or Brownfield ($299) at application time (auto-applied via `ref_code`).
- **Referrer gets $100 credit** applied to their next Greenfield or Brownfield once the referred party completes their first paid sprint OR their Skool 7-day-trial-to-paid conversion.
- Never expires. One credit per referred person. No leaderboard, no gamification.

**Copy (source of truth for `/community` and any referral landing):**
> Bring a builder. Both of you get $100 off. They apply with your link, we accept, they pay $99 for a Greenfield or $299 for a Brownfield. When they finish, your next sprint is $100 off. Open to anyone — Ship Club member, sprint grad, first-time visitor. Never expires. Applies once per referred person.

**Tracked in:** application email + acceptance workflow. Applied at Stripe payment link stage (Will manually generates the discounted link during acceptance).

---

*Owner: Will. One person merges changes. Version this file.*
