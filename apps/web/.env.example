NEXT_PUBLIC_META_PIXEL_ID=
# Public origin used by /api/talk to forward capture_lead to /api/lead. Set to https://www.stuck.builders in prod; falls back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=
RESEND_API_KEY=
APPLICATIONS_EMAIL=will@stuck.builders
APPLICATIONS_FROM_EMAIL=will@stuck.builders
ANTHROPIC_API_KEY=
# Optional Slack webhook for /api/talk notify_will tool. If unset, hot-visitor pings log to console instead.
SLACK_WEBHOOK_URL=
